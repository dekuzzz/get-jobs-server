# 🎯 快速设置指南

## 数据库信息
- **PostgreSQL**: 15
- **密码**: `WCwsad123456`
- **地址**: `localhost:5432`

---

## 🚀 一键执行（推荐）

我已经为您创建了自动化脚本，只需运行：

```bash
./setup_and_run.sh
```

这个脚本会自动：
1. ✅ 检查并启动PostgreSQL服务
2. ✅ 执行数据库迁移（添加role字段）
3. ✅ 检查Java版本
4. ✅ 启动应用服务
5. ✅ 验证服务状态

---

## 📝 手动执行步骤

如果自动脚本无法运行，可以手动执行：

### 1. 启动PostgreSQL

```bash
# 使用Homebrew启动
brew services start postgresql@15

# 验证服务
lsof -i :5432
```

### 2. 执行数据库迁移

```bash
# 方式A：使用Python脚本
python3 migrate_database.py

# 方式B：使用便捷脚本
./run_migration.sh
```

### 3. 启动应用服务

**方式A：使用Docker（推荐，无需Java 17）**
```bash
docker-compose up -d
```

**方式B：使用Maven（需要Java 17）**
```bash
mvn spring-boot:run
```

**方式C：运行JAR（需要Java 17）**
```bash
mvn clean package -DskipTests
java -jar target/backend-1.0.jar
```

### 4. 测试接口

```bash
./test_api.sh
```

---

## ✅ 验证清单

- [ ] PostgreSQL服务运行中（端口5432）
- [ ] 数据库迁移已执行（role字段已添加）
- [ ] 应用服务运行中（端口8080）
- [ ] 接口测试通过

---

## 🔧 配置文件位置

- **数据库配置**: `src/main/resources/application.properties`
- **迁移脚本**: `migrate_database.py`
- **测试脚本**: `test_api.sh`

---

## 📞 需要帮助？

查看详细文档：
- `START_HERE.md` - 详细开始指南
- `EXECUTION_SUMMARY.md` - 执行总结
- `QUICK_START.md` - 快速开始

---

**现在就开始：运行 `./setup_and_run.sh` 即可！** 🎉

